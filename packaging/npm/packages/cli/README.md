# @factile/cli

This scoped package is an alias for the canonical `factile` npm package.

```bash
npm install -g @factile/cli
factile version
factile init
```

`factile init` is the normal repository onboarding and repeat repair command.

Primary installation docs use the unscoped package:

```bash
npm install -g factile
```
